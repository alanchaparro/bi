name: Docker CI

on:
  push:
    branches:
      - main
      - master
      - develop
      - 'feature/**'
  pull_request:
  workflow_dispatch:

jobs:
  docker-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Compose Version
        run: docker compose version

      - name: Build Image
        run: docker compose --profile dev build --no-cache dashboard api-v1

      - name: Start Services (Legacy + API v1)
        run: docker compose --profile dev up -d dashboard api-v1

      - name: Compile Check
        run: docker compose --profile dev exec -T dashboard python -m py_compile start_dashboard.py

      - name: Unit Tests
        run: |
          set -euo pipefail
          set +e
          docker compose --profile dev exec -T dashboard sh -lc "pwd && ls -la tests && python -m unittest discover -s tests -p 'test_*.py'" > /tmp/unit-tests.log 2>&1
          rc=$?
          set -e
          cat /tmp/unit-tests.log
          if [ "$rc" -ne 0 ]; then
            short="$(head -n 30 /tmp/unit-tests.log | tr '\n' ' ' | tr -s ' ')"
            echo "::error::Unit Tests failed: ${short}"
            exit "$rc"
          fi

      - name: API v1 Contract Tests
        run: docker compose --profile dev exec -T dashboard python -m unittest tests.test_api_v1_brokers_config -v

      - name: Smoke - Health Endpoints
        shell: bash
        run: |
          set -euo pipefail
          curl -fsS "http://localhost:5000/api/check-files" > /tmp/check-files.json
          curl -fsS "http://localhost:5000/" > /tmp/index.html
          grep -q "dashboard.js" /tmp/index.html
          curl -fsS "http://localhost:8000/api/v1/health" > /tmp/v1-health.json
          grep -q "\"ok\":true" /tmp/v1-health.json

      - name: Export OpenAPI v1
        run: docker compose --profile dev exec -T dashboard python scripts/export_openapi_v1.py

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Frontend Install
        working-directory: frontend
        run: npm ci

      - name: Generate OpenAPI TS Types
        working-directory: frontend
        run: npm run generate:types

      - name: Frontend Typecheck + Build
        working-directory: frontend
        run: |
          npm run typecheck
          npm run build

      - name: Logs on Failure
        if: failure()
        run: |
          docker compose logs dashboard --tail=400
          docker compose --profile dev logs api-v1 --tail=400

      - name: Cleanup
        if: always()
        run: docker compose down -v
